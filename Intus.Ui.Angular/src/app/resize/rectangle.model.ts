export class Rectangle {
  height: number;
  width: number;
  x: number;
  y: number;

  constructor(height: number, width: number, x: number,   y:number) {
    this.height = height;
    this.width = width;
    this.x = x;
    this.y = y
  }
}
